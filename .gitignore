# build output
dist/
.astro/

# dependencies
node_modules/

# environment — never commit real values
.env
.env.production
.env.local

# ingesta: insumos regenerables, no fuente de verdad (SPEC §6.2)
scripts/import/entrada/
scripts/import/cache/
scripts/import/reporte-*.md

# derivadas locales para trabajar sin red. Las de verdad viven en R2 desde la
# fase 2.1; esto es salida de scripts/dev/imagenes-locales.mjs
public/img-dev/

# cloudflare
.wrangler/

# estado generado por herramientas locales (registry de skills)
.atl/

# editor / OS
.DS_Store
Thumbs.db
*.swp

# admin: build y dependencias propias (proyecto aparte, SPEC-etapa2 4.1)
admin/dist/
admin/.astro/
admin/node_modules/
admin/.dev.vars
